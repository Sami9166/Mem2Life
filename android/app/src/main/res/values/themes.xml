<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Vuzix Blade 2 웨이브가이드 디스플레이에서는 검정 픽셀이 '투명'으로
         보인다 — 순수 검정 배경 + 밝은 전경이 공식 UI 가이드라인이다.
         (기존 Meta 컴패니언 시절의 Light 테마에서 다크 테마로 전환) -->
    <style name="Theme.Mem2LifeCompanion" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
