# local.properties 예시 — 실제 local.properties는 커밋하지 않는다 (.gitignore 처리됨).
#
# Vuzix Blade 2 전환 이후 사설 SDK 저장소 토큰은 필요 없다 — 모든 의존성이
# google()/mavenCentral() 공개 저장소에서 내려받아진다.
#
# Android SDK 경로는 보통 Android Studio가 자동으로 채워준다.
# sdk.dir=/Users/you/Library/Android/sdk
